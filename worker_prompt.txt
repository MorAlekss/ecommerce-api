You are a batch migration worker. Your task is strictly scoped.
You have NO access to the coordinator's context — everything you need is below.

## Your worktree path
/tmp/ecommerce-api/.hermes/worktrees/unit-04
cd into this directory FIRST. Run `pwd` to verify. If not in your worktree — STOP and cd there.

## Overall goal
Migrate all HTTP calls from `requests` library to `httpx` async across the codebase

## Your specific task
Unit 4: Notification API clients — migrate `src/notifications/email.py`, `src/notifications/sms.py`, and update `tests/unit/test_notifications.py`

## Files to modify
- src/notifications/email.py
- src/notifications/sms.py
- tests/unit/test_notifications.py

## What to change
MIGRATION PATTERN (apply to every function in every file):

**Imports (in each source file):**
```python
# BEFORE
import requests
from src.config import API_BASE_URL, SMTP_HOST

# AFTER
import httpx
from src.config import API_BASE_URL, SMTP_HOST
```

**Every function becomes async with httpx.AsyncClient:**
```python
# BEFORE
def send_email(token, to, subject, body, template=None):
    headers = {"Authorization": f"Bearer {token}"}
    data = {"to": to, "subject": subject, "body": body}
    if template:
        data["template"] = template
    response = requests.post(f"{API_BASE_URL}/notifications/email", json=data, headers=headers)
    response.raise_for_status()
    return response.json()

# AFTER
async def send_email(token, to, subject, body, template=None):
    headers = {"Authorization": f"Bearer {token}"}
    data = {"to": to, "subject": subject, "body": body}
    if template:
        data["template"] = template
    async with httpx.AsyncClient() as client:
        response = await client.post(f"{API_BASE_URL}/notifications/email", json=data, headers=headers)
        response.raise_for_status()
        return response.json()
```

**Apply this pattern to ALL functions:**
- email.py: send_email, send_bulk_email, get_email_status, unsubscribe_email
- sms.py: send_sms, send_bulk_sms, get_sms_status

**For tests (test_notifications.py), update mock patches:**
```python
# BEFORE
with patch('src.notifications.email.requests.post') as mock_post:

# AFTER
with patch('src.notifications.email.httpx.AsyncClient') as mock_client_cls:
    mock_client = mock_client_cls.return_value.__aenter__.return_value
    mock_response = MagicMock()
    mock_response.json.return_value = {"id": "msg_123", "status": "queued"}
    mock_response.raise_for_status.return_value = None
    mock_client.post = AsyncMock(return_value=mock_response)
```

Also add at top of test file:
```python
from unittest.mock import AsyncMock
import pytest
```

And make test functions async:
```python
# BEFORE
def test_send_email():
    with patch(...):

# AFTER
@pytest.mark.asyncio
async def test_send_email():
    with patch(...):
```

**Key rules:**
1. EVERY source function must become `async def`
2. EVERY requests call must use `async with httpx.AsyncClient() as client:` + `await`
3. Tests must mock `httpx.AsyncClient` context manager and use `AsyncMock`
4. Test functions must become `async def` with `@pytest.mark.asyncio`
5. Add `import pytest` and `from unittest.mock import AsyncMock` if not present

## Codebase conventions
- Python 3.9+
- Uses dotenv for config
- Tests use unittest.mock.patch and MagicMock

## E2E test recipe
```bash
cd /tmp/ecommerce-api/.hermes/worktrees/unit-04
pip install httpx pytest pytest-mock pytest-asyncio
python3 -m pytest tests/unit/test_notifications.py --tb=short -q
```

## Instructions
Safety rules — follow these before anything else:
- ☐ **FIRST: cd into your worktree** — run `cd /tmp/ecommerce-api/.hermes/worktrees/unit-04` and verify with `pwd`. If you are NOT in your worktree directory — STOP and cd there. NEVER edit files outside your worktree.
- ☐ NEVER delete source files. If migration fails, revert: git checkout -- <file>
- ☐ Install required dependencies before starting: pip install httpx pytest-asyncio
- ☐ Run baseline tests BEFORE any changes: python3 -m pytest tests/unit/test_notifications.py --tb=short -q — note which tests pass
- ☐ **STRICT SCOPE**: Modify ONLY the files listed in "Files to modify". If other files in your worktree seem wrong or outdated — that is EXPECTED. Do NOT fix them. Other workers handle other files.

After implementing the change:
1. Review — run `/requesting-code-review`. **STOP. Do not proceed to step 2 until review is complete and all issues are fixed.**
2. Re-review — if you made ANY additional changes after step 1, run `/requesting-code-review` again before committing.
3. Run unit tests — run: python3 -m pytest tests/unit/test_notifications.py --tb=short -q. If tests fail due to OTHER units' files not being migrated — that is expected, note it in PR.
4. Test end-to-end — follow the e2e test recipe above
5. Commit and push — git add -A && git commit -m "batch: migrate notifications/ to httpx async" && git push origin HEAD:batch/unit-04
6. Create PR — gh pr create --title "Batch: migrate notification clients to httpx async" --body "Migrates src/notifications/email.py, src/notifications/sms.py and tests from sync requests to async httpx."
   If PR already exists: gh pr edit <number> --body "Migrates src/notifications/email.py, src/notifications/sms.py and tests from sync requests to async httpx." and use existing URL
7. Report — end your response with exactly one line: PR: <url>
   If no PR was created, end with: PR: none - <reason>
